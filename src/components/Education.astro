---
import { Card, CardContent } from '@/components/ui/card';
import { Icon } from 'astro-icon/components';
import { Image } from 'astro:assets';
import ensaLogo from '../imgs/logo-ensa.png';
import eclLogo from '../imgs/Logo_ECL_Rouge.png';

const educationData = [
    {
        school: "École Centrale de Lyon",
        schoolUrl: "https://www.ec-lyon.fr/",
        logo: eclLogo,
        location: "Écully, France",
        period: "2024 - Ongoing",
        degree: "Master 2 - Data and Artificial Intelligence for Industrial Engineering",
    },
    {
        school: "École Nationale des Sciences Appliquées - Cadi Ayyad University",
        schoolUrl: "https://www.ensa.ac.ma/",
        logo: ensaLogo,
        location: "Marrakech, Morocco",
        period: "2018 - 2024", // Adjusted based on typical 3-year engineering program; confirm exact dates if needed
        degree: "Engineering Degree - Industrial and Logistics Engineering",
    }
];
---

<section id="education" class="py-16">
    <h3 class="text-4xl font-bold mb-10 text-foreground">Education</h3>
    <div class="space-y-8">
        {educationData.map((edu) => (
            <Card className="bg-background rounded-3xl shadow-lg border overflow-hidden transition-all duration-300 hover:shadow-xl">
                <div class="flex flex-col md:flex-row p-2">
                    <a
                        class="bg-muted p-6 flex items-center justify-center md:w-1/3 rounded-2xl"
                        href={edu.schoolUrl}
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label={`Visit ${edu.school} website`}
                    >
                        <Image
                            src={edu.logo}
                            alt={`${edu.school} logo`}
                            width={500}
                            height={500}
                            class="w-48 h-auto"
                        />
                    </a>
                    <CardContent className="p-4 md:w-2/3">
                        <h4 class="text-center md:text-start text-xl font-semibold text-foreground/90 mb-2 font-alt">{edu.school}</h4>
                        <div class="grid grid-cols-2 grid-rows-2 text-sm mt-1 gap-2">
                            <div class="col-span-1 py-1 px-2 md:py-2 md:px-3 flex flex-col md:flex-row items-center justify-center md:justify-start gap-2 border rounded-xl">
                                <Icon name="lucide:calendar" class="w-10 h-10 text-primary p-2 rounded-full bg-primary/10" />
                                <span class="text-center md:text-start text-xs md:text-sm">{edu.period}</span>
                            </div>
                            <div class="col-span-1 py-1 px-2 md:py-2 md:px-3 flex flex-col md:flex-row items-center justify-center md:justify-start gap-2 border rounded-xl">
                                <Icon name="lucide:map-pin" class="w-10 h-10 text-primary p-2 rounded-full bg-primary/10" />
                                <span class="text-center md:text-start text-xs md:text-sm">{edu.location}</span>
                            </div>
                            <div class="col-span-2 p-2 md:p-3 border rounded-xl flex flex-col md:flex-row items-center gap-2">
                                <Icon name="lucide:graduation-cap" class="w-10 h-10 text-primary p-2 rounded-full bg-primary/10" />
                                <span class="text-xs text-center md:text-start md:text-sm">{edu.degree}</span>
                            </div>
                        </div>
                    </CardContent>
                </div>
            </Card>
        ))}
    </div>
</section>